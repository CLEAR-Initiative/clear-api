services:
  api:
    build:
      context: .
      target: base
    command: bun run dev
    ports:
      - "4000:4000"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
